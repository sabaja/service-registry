server:
  port:8761

spring:
  application:
    name:service-registry

eureka:
  client:
    register-with-eureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/